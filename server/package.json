{
  "name": "handy-license-server",
  "version": "1.0.0",
  "description": "Handy 许可证服务器",
  "main": "src/index.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute handy-licenses --file=./schema.sql",
    "db:init:local": "wrangler d1 execute handy-licenses --local --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.24.0"
  }
}
